import { FullViewDirective } from './full-view.directive';

describe('FullViewDirective', () => {
  it('should create an instance', () => {
    const directive = new FullViewDirective();
    expect(directive).toBeTruthy();
  });
});
